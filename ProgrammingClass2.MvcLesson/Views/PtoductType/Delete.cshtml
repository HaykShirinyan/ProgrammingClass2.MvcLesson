@model ProductType

<div>
    <dl>
        <dt>
            ID
        </dt>
        <dd>
            @Model.Id
        </dd>

        <dt>
            Name
        </dt>
        <dd>
            @Model.Name
        </dd>

        <dt>
            Description
        </dt>
        <dd>
            @Model.Description
        </dd>
    </dl>

    <p>
        Are you sure you want to delete this productType?
    </p>

    <form asp-controller="ProductTypes" asp-action="Delete" asp-route-id="@Model.Id">
        <div class="text-center">
            <button type="submit" class="btn btn-primary">
                Yes
            </button>

            <a asp-controller="ProductTypes"
               asp-action="Index"
               class="btn btn-secondary">
                No
            </a>
        </div>
    </form>
</div>